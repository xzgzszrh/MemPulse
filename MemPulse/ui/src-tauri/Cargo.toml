[package]
name = "mempulse-desktop"
version = "0.2.0"
description = "MemPulse local memory workspace"
authors = ["MemPulse"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-window-state = "2"
serde_json = "1"
